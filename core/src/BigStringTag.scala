package ltbs.uniform

/** Tagged type used to denote a string that could be more than a
  * single line in some way. For example creating a textarea rather
  * than a string field 
  */
trait BigStringTag

